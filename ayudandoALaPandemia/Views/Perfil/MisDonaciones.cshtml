
@{
    ViewBag.Title = "MisDonaciones";
    Layout = "~/Views/Shared/_NecesidadesLayout.cshtml";
}

<h2>MisDonaciones</h2>

<div class="container">

    <!-- Page Heading/Breadcrumbs -->
    <h1 class="mt-4 mb-3">
    </h1>

    <div class="mb-4" id="accordion" role="tablist" aria-multiselectable="true">
        <div class="card">
            <div class="card-header" role="tab" id="headingOne">
                <h5 class="mb-0">
                    <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne" aria-expanded="true" aria-controls="collapseOne">Historial de donaciones</a>
                </h5>
            </div>

            <div id="collapseOne" class="collapse show" role="tabpanel" aria-labelledby="headingOne">
                <div class="card-body">
                    <div class="row"><!--
                        @if (ViewBag.Necesidades != null)
                        {

                            foreach (var p in ViewBag.Necesidades)
                            {
                                <div class="col-lg-4 mb-4">
                                    <div class="card h-100">
                                        <h3 class="card-header">@p.Nombre</h3>
                                        <div class="card-body">
                                            <h2 class="font-italic">@p.FechaCreacion</h2>
                                            @if (@p.TipoDonacion == 1)
                                            {
                                                <h2 class="font-italic">Monetaria</h2>
                                            }
                                            else
                                            {
                                                <h2 class="font-italic">Insumos</h2>
                                            }
                                        </div>
                                        <ul class="list-group list-group-flush">
                                            @switch (@p.Estado)
                                            {
                                                case 1:
                                                    <li class="list-group-item">Activo</li>
                                                    break;
                                                case 2:
                                                    <li class="list-group-item">Inactivo</li>
                                                    break;
                                                case 0:
                                                    <li class="list-group-item">Inactivo</li>
                                                    break;
                                            }

                                            <li class="list-group-item">@ViewBag.Total</li>

                                            <li class="list-group-item">
                                                <a href="#" class="btn btn-primary">Ir a detalle</a>
                                            </li>
                                        </ul>
                                    </div>
                                </div>
                            }
                        }-->
                    </div>
                    <!-- /.row -->
                </div>
            </div>
        </div>
    </div>
</div>